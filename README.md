# emv-fleet airlock drill (throwaway)

One-off rehearsal for the codetask Human Airlock: GitHub App identity,
ruleset deny matrix, and a full autonomous-task lifecycle. Safe to delete.

- `calc.py` contains an intentionally planted bug in `add()`.
- CI runs `pytest`; the `quality` check must be green to merge.
